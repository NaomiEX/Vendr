class order {
  constructor(id, orderDate, orderId, items, total) {
    this.id = id;
    this.orderDate = orderDate;
    this.orderId = orderId;
    this.items = items;
    this.total = total;
  }
}

export default order;
