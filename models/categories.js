class category {
  constructor(id, icon, imageUrl, title, color) {
    this.id = id;
    this.icon = icon;
    this.imageUrl = imageUrl;
    this.title = title;
    this.color = color;
  }
}

export default category;
