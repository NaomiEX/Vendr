class Product {
  constructor(id, ownerId, title, imageUrl, categories, description) {
    this.id = id;
    this.ownerId = ownerId;
    this.title = title;
    this.imageUrl = imageUrl;
    this.categories = categories;
    this.description = description;
  }
}

export default Product;
